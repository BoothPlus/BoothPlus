import ProductInfo from '../ProductInfo/ProductInfo';
import * as S from './FloatingPanel.styles';

const FloatingPanel = () => {
  return (
    <S.FloatingPanel>
      <ProductInfo />
      카드 코멘트들 코멘트작성
    </S.FloatingPanel>
  );
};

export default FloatingPanel;
